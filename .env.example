# Server-side only. NEVER prefix with NEXT_PUBLIC_ — that would ship it to the browser.
# Used exclusively by src/app/api/demo/* route handlers, which run on the server.
RAPIDAPI_KEY=

# Public canonical origin used by sitemap.ts / robots.ts / metadataBase.
NEXT_PUBLIC_SITE_URL=https://developers.flightpowers.com
