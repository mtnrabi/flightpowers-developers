/**
 * Shared constants for the /llms.txt and /llms-full.txt machine-readable
 * surfaces. Route files cannot export arbitrary symbols, so this lives here.
 */

/** Stamped manually when the generated llms.txt content meaningfully changes. */
export const LLMS_LAST_UPDATED = '2026-08-27';
